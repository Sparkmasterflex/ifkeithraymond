%div{:class => "#{object.class.to_s.downcase} images"}
  - if object.has_images?
    %ul#slides
      = render :partial => '/images/image_edit', :collection => ordered_images(object.images), :as => :image, :locals => {:object => object, :is_project => object.is_a?(Project)}
